<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="AutoCompleteWindowsReference.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewmodels="clr-namespace:AutoCompleteWindowsReference.ViewModels"
    Title="{Binding Title}"
    x:DataType="viewmodels:MainViewModel">
    <ContentPage.Content>
        <StackLayout
            Padding="20"
            HorizontalOptions="Center"
            VerticalOptions="Center">

            <Label Text="This sample app is not relevant to the main issue. To test this issue please run the unit test project. When running on windows the tests will pass but running them on macOS will cause the following exception:" />

            <Button Command="{Binding DoSomethingCommand}" Text="Do Something" />

            <Button Command="{Binding NavigateCommand}" Text="Navigate" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
